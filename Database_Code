create table donor(NAME VARCHAR2(20),
AGE NUMBER(4),
GENDER VARCHAR(10),
ADDRESS VARCHAR(50),
CONTACTNO NUMBER(10),
ID NUMBER);

CREATE TABLE BLOOD(
BLOODGROUP CHAR(3),
PLATELET NUMBER,
RBC NUMBER,
REGDATE DATE,
ID NUMBER);

select * from donor;

select * from blood;
